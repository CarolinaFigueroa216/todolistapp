
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="@color/lilaPastel"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/tvNombreUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Usuario"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/azulPastel"
        android:gravity="center"
        android:layout_marginBottom="4dp"/>

    <TextView
        android:id="@+id/tvRolUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Rol: USER"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/morado"
        android:gravity="center"
        android:layout_marginBottom="16dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAgregar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="➕ Agregar tarea"
        android:layout_marginBottom="12dp"
        android:backgroundTint="@color/rosaPastel"/>

    <!-- Panel de información de tareas pendientes -->
    <LinearLayout
        android:id="@+id/panelTareasPendientes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/panel_info_background"
        android:padding="12dp"
        android:layout_marginBottom="12dp">

        <TextView
            android:id="@+id/tvTituloPendientes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📋 Tareas Pendientes"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/morado"
            android:layout_marginBottom="8dp"/>

        <!-- ScrollView para permitir desplazamiento en la lista de pendientes -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxHeight="200dp"
            android:fillViewport="false">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerPendientes"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false"/>
        </ScrollView>

        <TextView
            android:id="@+id/tvTareasPendientesInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 tareas pendientes"
            android:textSize="14sp"
            android:textColor="@color/morado"
            android:layout_marginTop="8dp"
            android:textStyle="bold"/>
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerTareas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCerrarSesion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🚪 Cerrar sesión"
        android:backgroundTint="@color/morado"/>
</LinearLayout>
